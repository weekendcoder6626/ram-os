export enum WindowAction {

    MINIMIZE,
    MAXIMIZE,
    CLOSE
}

export enum WindowState {

    MINIMIZED,
    MAXIMIZED,
}

export enum WindowType {
    ABOUT_ME,
    ABOUT_WORK,
    HIRE_ME,
    CONTACT_ME,
    COMMAND_PROMPT,
    RECYCLING_BIN,
}